
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

autogenerated = 0 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
#monitor=,preferred,auto,auto
monitor = eDP-1, 1920x1080@60.05Hz, 1920x0, 1
monitor = HDMI-A-1, 1920x1080@60.00Hz, 0x0, 1

# monitor = eDP-1, 1920x1080@120, 1920x1080, 1
# monitor = HDMI-A-1, 1920x1080@60, 0x1080, 1
# monitor = eDP-1, highres, highrr, auto, 1
# monitor = HDMI-A-1, highres, highrr, auto, 1
# monitor = eDP-1, highrr, auto, 1
# monitor = HDMI-A-1, highrr, auto, 1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nautilus
$menu = wofi --show drun
$browser = floorp
$vscode = code


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = hyper-session start


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,32
env = HYPRCURSOR_SIZE,32


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 5

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgb(cba6f7) rgb(f38ba8) rgb(eba0ac) rgb(fab387) rgb(f9e2af) rgb(a6e3a1) rgb(94e2d5) rgb(89dceb) rgb(89b4fa) rgb(b4befe) 270deg
    col.inactive_border = rgb(45475a) rgb(313244) rgb(45475a) rgb(313244) 270deg

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    extend_border_grab_area = 10

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 8

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0
    fullscreen_opacity = 1.0

    #Subtitle shadows
    col.shadow = rgba(11111baf)
    col.shadow_inactive = rgba(1e1e2eaf)

    dim_inactive = true
    dim_strength = 0.025

    drop_shadow = true
    shadow_range = 304
    shadow_render_power = 4
    shadow_offset = 0, 42
    shadow_scale = 0.9
    col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 2
        passes = 6
        ignore_opacity = true
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    force_split = 1
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

group {
    groupbar {
        font_family = Work Sans
        font_size = 12
        gradients = true
    }
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    disable_autoreload = false;
    disable_splash_rendering = false;
    disable_hyprland_logo =  true;
    background_color = rgb(30, 30, 46)
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false; # If true disables the random hyprland logo / anime girl background. :(
    mouse_move_enables_dpms = true; # enable dpms on mouse/touchpad action
    key_press_enables_dpms = true; # enable dpms on keyboard action
    animate_manual_resizes = true;
    animate_mouse_windowdragging = true;
    mouse_move_enables_dpms = true; # enable dpms on mouse/touchpad acti
    key_press_enables_dpms = true; # enable dpms on keyboard acti
    # Unfullscreen when opening something
    new_window_takes_over_fullscreen = 2
    vfr = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = br
    kb_variant = abnt2
    kb_model = pc105
    kb_options =
    kb_rules =

    follow_mouse = 2
    repeat_rate = 30
    repeat_delay = 300

    sensitivity = 0.7 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
        disable_while_typing = true
        clickfinger_behavior = true
        tap-and-drag = true
    }

    accel_profile = adaptive
    numlock_by_default = true
    sensitivity = 0.7 # -1.0 - 1.0, 0 means no modification.
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
    workspace_swipe_fingers = 3
    workspace_swipe_forever = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

################
### Xwayland ###
################
# unscale XWayland
xwayland {
    force_zero_scaling = true
}
####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, B, exec, $browser
bind = $mainMod, T, exec, $vscode
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.
# only allow shadows for floating windows
windowrulev2 = noshadow, floating:0
# make floating windows opaque
windowrulev2 = opacity 0.72, floating:1
# Some windows should never be opaque
windowrulev2 = opacity 1.0, class: com.obsproject.Studio
windowrulev2 = opacity 1.0, class: resolve
windowrulev2 = opacity 1.0, class: com.defold.editor.Start
windowrulev2 = opacity 1.0, class: class: dmengine
windowrulev2 = opacity 1.0, title: UNIGINE Engine

# make pop-up file dialogs floating, centred, and pinned
windowrulev2 = float, title:(Open|Progress|Save File)
windowrulev2 = center, title:(Open|Progress|Save File)
windowrulev2 = pin, title:(Open|Progress|Save File)
windowrulev2 = float, class:(xdg-desktop-portal-gtk)
windowrulev2 = center, class:(xdg-desktop-portal-gtk)
windowrulev2 = pin, class:(xdg-desktop-portal-gtk)
windowrulev2 = float, class:^(code)$
windowrulev2 = center, class:^(code)$
windowrulev2 = pin, class:^(code)$

# Apps that should be floating
windowrulev2 = float, title:(Maestral Settings|MainPicker|overskride|Pipewire Volume Control|Trayscale)
windowrulev2 = center, title:(Maestral Settings|MainPicker|overskride|Pipewire Volume Control|Trayscale)
windowrulev2 = float, initialTitle:(Polychromatic|Syncthing Tray)
windowrulev2 = center, initialTitle:(Polychromatic|Syncthing Tray)
windowrulev2 = float, class:(.blueman-manager-wrapped|blueberry.py|nm-connection-editor|org.gnome.Calculator|polkit-gnome-authentication-agent-1)
windowrulev2 = center, class:(.blueman-manager-wrapped|blueberry.py|nm-connection-editor|org.gnome.Calculator|polkit-gnome-authentication-agent-1)
windowrulev2 = size 700 580, title:(.blueman-manager-wrapped)
windowrulev2 = size 580 640, title:(blueberry.py)
windowrulev2 = size 600 402, title:(Maestral Settings)
windowrulev2 = size 512 290, title:(MainPicker)
windowrulev2 = size 395 496, class:(org.gnome.Calculator)
windowrulev2 = size 700 500, class:(nm-connection-editor)
windowrulev2 = size 1134 880, title:(Pipewire Volume Control)
windowrulev2 = size 960 640 initialTitle:(Polychromatic)
windowrulev2 = size 880 1010, title:(overskride)
windowrulev2 = size 886 960, title:(Trayscale)

###################
### Layer rules ###
###################
layerrule = blur, launcher # fuzzel
layerrule = ignorezero, launcher
layerrule = blur, logout_dialog # wlogout
layerrule = blur, rofi
layerrule = blur, swaync-control-center
layerrule = blur, swaync-notification-window
layerrule = ignorealpha 0.7, swaync-control-center
layerrule = ignorealpha 0.7, swaync-notification-window
